; Real-terminal scenarios: the built TUI inside tmux, panes captured and
; compared by cram (dune promote re-records). Skipped when tmux is absent.

(cram
 (deps %{bin:prigh-tui})
 (enabled_if %{bin-available:tmux}))
